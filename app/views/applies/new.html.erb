<%= form_tag :controller => "applies", :action => "create" do %>
  <% if @apply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apply.errors.count, "error") %> prohibited this administrator from being saved:</h2>

      <ul>
      <% @apply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h4>Você está se candidatando a vaga: <%= @job.title %>, tem certeza?</h4>
  <%= hidden_field_tag :job_id, @job.id %>
  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :resume_id, current_user.resume.id %>
  <%= link_to_submit content_tag(:span, "Confirmar"), :class => "btn btn-danger" %>
<% end %>