<%= form_tag :controller => "applies", :action => "create" do %>
  <%= hidden_field_tag :user_id, :value => current_user.id %>
  <%= hidden_field_tag :resume_id, :value => current_user.resume.id %>
  <%= f.submit "Aplicar" %>
<% end %>